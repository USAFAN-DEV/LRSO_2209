ls: lista todos los subdirectorios/ficheros que hay
cd: para ir a un directorio. cd / para el directorio raiz, cd ~ para el directorio home, cd . directorio actual, cd .. directorio padre
ej cd music
cd .. :para retroceder al directorio anterior
pwd: te dice donde estas situado actualmente
ls -a : te muestra también los ficheros ocultos
ls -l: te da una serie de datos sobre tus ficheros. LETRAS: 1 caracter. d directorio, si no otra cosa (fichero); 
siguientes tres caracteres se refieren al propietario del fichero, y te dicen que derechos tiene el propietario 
sobre ese fichero rwx (read, write, execute). siguientes tres caracteres se refieren al grupo. ultimos tres caracteres
se refieren a cualquier usuario del sistema. numerito sig :cuantos subelementos contiene el subdirectorio. numerito despues del usuario y el grupo:
tamaño en bytes del fichero/directorio
despues fecha de creación y el nombre del directorio.
ls -al :te muestra estos datos de los ficheroso cultos y de los directorios
clear para limpiar el terminal
ls -R: te dice los directorios y subdirectorios
history: te muestra los comandos que has ejecutado
file /djflksfjslk : tipo de archivo que es
type: para cambiar el nombre de un comando
which :te muestra la ruta para llegar a un archivo (te muestra el path)
echo $PATH: te muestra el path entero
echo: para mostrar algo
printenv : te muestra todas las variables del s.o.
printenv variableentorno/echo $variable entorno; para ver una variable del s.o.
cat : muestra en consola el contenido del fichero
ctr+r para buscar comandos que ya has usado 
man  comando : manual de ese comando
mkdir nombre : para crear un directorio
mkdir -p path nombredirectorio: Crear un directorio y subdirectorios con un unico comando
rmdir nombre : borrar un directorio sin archivos
rm -r nombre directorio: para borrar una carpeta y todos sus archivos
touch nombre : para crear un ficherol
rm nombre fichero: para borrar un fichero
echo "....."> desc.txt; para escribir algo dentro de un fichero/echo ".......">>desc.txt; para escribir algo en un fichero sin borrar lo que 
habias escrito antes
cat desc.txt; para leer un fichero
less desc.txt, Para entrar en un dichero y /patern para buscar un patron>
tail -n10 nombre fichero, te muestra las ultimas 10 lineas de un fichero
head -n10 nombre fichero, te muestra las 10 primeras lineas de un fichero

ln -s fichero nombre (a donde quiero llegar con mi acceso directo) nombre del acceso directo

cp  nombrefichero  directorio al que queremos copiarlo; para copiar un fichero desde un directorio hasta otro
cp *txt directorio; para copiar todos los archivos que acaban en txt a una carpeta
mv nombre.txt nuevonombre.txt: para cambiar el nombmre de un fichero
mv nombre fichero directorio al que queremos mandarlo: para cortar y pegar un fichero en otro directorio
tar cvf nombrefichero.tar cadaunodelosficherosquequeremosempaquetar
tar cvfz mitar.tgz cadaunodelosficherosquequeremosempaquetar: crear un tar pero comprimido para que no ocupe mucho espacio (compresión tipo gzip)
tar cvjf mibzip.bz2 ficheroquequeremosempaquetar, compresión tipo bzip
tar xvf ldlfjsk.tar; para descomprimir un tar
tar xvfz mitar.tgz; para descomprimir un tar tipo tgz
gzip nombrefichero; para convertir un fichero en un archivo zip
bzip2 nombrefichero; para convertir un fichero en un archivo zip
gunzip nombrearchivozip: para descomprimir el archivo zip
bunzip2 nombrearchivozip: para descomprimir el archivo zip

>
